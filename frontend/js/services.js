// Services page is now a CATEGORY landing page

document.addEventListener("DOMContentLoaded", () => {
    console.log("Services page loaded - category view");

    // Optional: animation or future features
});